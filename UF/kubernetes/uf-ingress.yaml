apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ct010-itax-uf-v1-ingress
  namespace: ct010-itax-uf-v1
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - tgadev.toruslowcode.com
      secretName: ct010-itax-uf-v1-tls
  rules:
    - host: tgadev.toruslowcode.com
      http:
        paths:
          - path: /ct010/i001/itax/v1
            pathType: Prefix
            backend:
              service:
                name: ct010-itax-uf-generator
                port:
                  number: 443