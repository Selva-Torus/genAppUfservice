apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ci001-a001-uf-v1-ingress
  namespace: ci001-a001-uf-v1
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - tgadev.toruslowcode.com
      secretName: ci001-a001-uf-v1-tls
  rules:
    - host: tgadev.toruslowcode.com
      http:
        paths:
          - path: /ci001/ag001/a001/v1
            pathType: Prefix
            backend:
              service:
                name: ci001-a001-uf-generator
                port:
                  number: 443