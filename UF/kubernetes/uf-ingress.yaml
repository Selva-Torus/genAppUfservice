apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: a001-uf-v1-ingress
  namespace: a001-uf-v1
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - tgadev.gsstvl.com
      secretName: a001-uf-v1-tls
  rules:
    - host: tgadev.gsstvl.com
      http:
        paths:
          - path: /ct266/ag001/a001/v1
            pathType: Prefix
            backend:
              service:
                name: a001-uf-generator
                port:
                  number: 443