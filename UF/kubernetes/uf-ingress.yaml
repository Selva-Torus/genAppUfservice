apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ct005-vgph001-uf-v1-ingress
  namespace: ct005-vgph001-uf-v1
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - tgadev.toruslowcode.com
      secretName: ct005-vgph001-uf-v1-tls
  rules:
    - host: tgadev.toruslowcode.com
      http:
        paths:
          - path: /ct005/v001/vgph001/v1
            pathType: Prefix
            backend:
              service:
                name: ct005-vgph001-uf-generator
                port:
                  number: 443