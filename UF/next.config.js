/** @type {import('next').NextConfig} */
const nextConfig = {
  //basePath: '',
   basePath: '/ct242/tob001/tob002/v1',
  reactStrictMode: false,
  output: 'standalone',  // Add this line to enable standalone builds
}

module.exports = nextConfig